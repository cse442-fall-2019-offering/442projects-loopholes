import "mocha";
import { expect } from "chai";
import { PostMetadata } from "../objects

describe("Newest", () => {
  it("newest post should come first", () =>
    const posts: PostMetadata[] {};
  );
  expect(posts[0].id == 1);
});
